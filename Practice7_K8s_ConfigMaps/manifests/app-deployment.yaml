apiVersion: apps/v1
kind: Deployment
metadata:
  name: configmap-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: configmap-demo
  template:
    metadata:
      labels:
        app: configmap-demo
    spec:
      containers:
        - name: app
          image: health-demo-p7:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: APP_GREETING
              valueFrom:
                configMapKeyRef:
                  name: app-config-p7
                  key: APP_GREETING
          volumeMounts:
            - name: config-volume
              mountPath: /etc/config
              readOnly: true
      volumes:
        - name: config-volume
          configMap:
            name: app-config-p7
            items:
              - key: message_key.txt
                path: message_file.txt